/// Constants used throughout the toast package.
class ToastConstants {
  /// Default animation duration
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);

  /// Default toast duration
  static const Duration defaultToastDuration = Duration(seconds: 3);

  /// Minimum swipe distance to trigger dismiss
  static const double minSwipeDistance = 50.0;

  /// Default border radius
  static const double defaultBorderRadius = 12.0;

  /// Default elevation
  static const double defaultElevation = 6.0;

  /// Default horizontal padding
  static const double defaultHorizontalPadding = 16.0;

  /// Default vertical padding
  static const double defaultVerticalPadding = 14.0;

  /// Default icon size
  static const double defaultIconSize = 24.0;

  /// Maximum toast width (for responsive design)
  static const double maxToastWidth = 400.0;

  /// Default margin from screen edges
  static const double defaultMargin = 16.0;

  /// Default spacing between icon and text
  static const double defaultIconSpacing = 12.0;

  /// Default spacing between text and action button
  static const double defaultActionSpacing = 8.0;
}
